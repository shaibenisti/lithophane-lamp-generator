# Style 3: BALANCED (Current Settings)
# Moderate enhancement with texture smoothing
# Result: Clear features with smooth skin

name: "Balanced"
description: "Moderate CLAHE + moderate smoothing"

clahe:
  enabled: true
  clip_limit: 1.5
  tile_size: [16, 16]

bilateral_filter:
  enabled: true
  diameter: 5
  sigma_color: 40
  sigma_space: 40

notes: "Current settings. Good all-around compromise."
